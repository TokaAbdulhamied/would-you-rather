import { _getUsers, _getQuestions } from "./_DATA";

export function getUser() {
  return _getUsers().then((users) => users);
}
export function getUser() {
  return _getQuestions().then((quistions) => quistions);
}
